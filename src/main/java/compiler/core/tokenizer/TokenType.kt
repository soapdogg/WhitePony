package compiler.core.tokenizer

enum class TokenType {
   BINARY_AND,
   BINARY_ASSIGN,
   BINARY_ASSIGN_OP,
   BINARY_OPERATOR,
   BINARY_OR,
   BINARY_RELATIONAL_OPERATOR,
   BIT_NEGATION,
   COMMA,
   DO,
   ELSE,
   FLOATING_POINT,
   FOR,
   IF,
   IDENTIFIER,
   INTEGER,
   LEFT_BRACE,
   LEFT_BRACKET,
   LEFT_PARENTHESES,
   PLUS_MINUS,
   PRE_POST,
   RETURN,
   RIGHT_BRACE,
   RIGHT_BRACKET,
   RIGHT_PARENTHESES,
   SEMICOLON,
   TYPE,
   UNARY_NOT,
   WHILE
}